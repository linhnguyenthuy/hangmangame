import PropTypes from 'prop-types'
import './HangMan.css'

import React from "react";

const Win = () => (
    <div className=".winner"><img src={`/hangman/win.gif`} />
    </div>
)

export default Win